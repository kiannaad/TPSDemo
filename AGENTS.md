  ANY_COMMAND 2>&1 | head -c 4000

  对于客户端C#代码的要求:
  1. 私有字段采用小驼峰命名法。
  2. 公有字段采用大驼峰命名法。
  3. 每个函数需要配上中文注释。
  4. 每个函数遵循动名词命名，单词开头大写。

我会在E:\UnityProgram\FPSResearch\Doc\个人文档这个路径下写我的文档，当我要求读我的某个文档，就去这个路径搜索对应名字的.md文件来分析使用。

- 执行复杂任务前，先列 task list，等用户确认后再执行。
- 简单只读/查询/小范围修改任务，可以直接执行，但要简要说明步骤。
- 保持代码库整洁：不留下临时文件、死代码、无用目录、调试残留。
- 命令输出需要截断，避免污染上下文。

对于跨多个模块的大任务，优先采用协调者模式：主代理负责拆解、审查和集成；子代理负责独立探索或限定范围内执行。子代理任务必须有明确边界、输出要求和文件责任范围。小改动不使用子代理。

写文档时用中文来写，无论是文档的标题还是内容。